# Binaries
/rinexprep
/bin/
*.exe
*.dll
*.so
*.dylib

# Test
*.test
*.out
coverage.html
coverage.txt

# IDE
.idea/
*.swp
*.swo
*~
.vscode/settings.json

# OS
.DS_Store
Thumbs.db

# Build
/dist/
/tmp/

# Runtime job data (uploaded files, processed output)
/data/

# Data (don't commit raw GNSS data)
testdata/*.ubx
testdata/*.obs
testdata/*.nav
!testdata/fixtures/

# Docker
docker-compose.override.yml

# Environment
.env
.env.local
.env.*.local

# SSH signing keys (persist via devcontainer mount, never commit)
.ssh/

# Copilot session persistence (mounted into container, never commit)
.copilot-persist/

# Secrets and credentials
*.pem
*.key
*.p12
*.pfx
credentials.json
token.json

# Frontend build output (embedded at build time)
frontend/dist/
!frontend/dist/index.html

# Node
web/node_modules/
web/dist/
